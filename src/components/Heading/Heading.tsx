import { FC } from 'react';
import { TitleImage, TitleWrapper } from './styles';

const Heading: FC = () => {
  return (
    <TitleWrapper>
      <TitleImage />
    </TitleWrapper>
  );
};

export default Heading;
